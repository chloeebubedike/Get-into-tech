#Iterated over and counted every letter in the string hello, using the len function
tup = 'Hello'
print(len(tup))

#Trailing comma turns string into tuple. Hello is counted as one element
tup = 'Hello',
print(len(tup))